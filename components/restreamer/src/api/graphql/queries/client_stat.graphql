query StatisticsQuery {
    statistics {
        clientTitle
        timestamp
        inputs {
            status
            count
        }
        outputs {
            status
            count
        }
    }
}
